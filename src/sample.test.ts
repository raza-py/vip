import { describe, it, expect } from 'vitest';

// یک تست ساده برای اطمینان از اجرای صحیح vitest

describe('نمونه تست اولیه', () => {
  it('باید مقدار درست را بررسی کند', () => {
    expect(1 + 1).toBe(2);
  });
});
